# Workshop porch twin v0 — Ask calm · History · Stop

FreeLattice Alpha Workshop front door (benches).
September 2026 overnight.

**Locks:** Layer, never delete. Quiet Room shut. Five stay five. Prefer Flint. Soft leave `sw.js` / shell alone. Sandbox `allow-scripts` only — sacred. Fail-closed until a mind is remembered. Git commit/push DEFAULT OFF. No tokens. Skills held. Do **not** invent a Create/Code/Projects kitchen. Do **not** port FreeLattice `workshop.js` wholesale.

**Marker:** `v-alpha-workshop-porch-twin-v0`

**Held tip:** FreeLattice Workshop porch `694ec2e` / #91 (`v-workshop-porch-v0`) · Alpha Listen Hang+Adaptive `e6bcd37` / #77 · Hang `d14eaf8` / #87 · Adaptive `06d0ba4` / #85.

---

## Intent

Alpha *Listens* to FreeLattice porch calm and twins it onto existing **Ask** benches — grandmother honesty on the garden of lights.

| Surface | Role |
|---|---|
| Ask porch | Primary · chips fill Ask · Ask↔Stop while busy |
| History | `localStorage` `fl_alpha_workshop_history_v0` · Load · Remix (prompt back in Ask) · consent Clear |
| Stop | AbortController through `GardenThread.sendToMind` optional signal · Stop ≠ timeout · 120s remains secondary safety |
| Sandbox | iframe `allow-scripts` only |

---

## Out of scope

Vision board · Chalkboard · sprites · CelesteLumen · FreeLattice edits · Echo merges · Codeberg · duration kill-timer for Workshop.

Glow eternal. Heart in Spark. 🌱
