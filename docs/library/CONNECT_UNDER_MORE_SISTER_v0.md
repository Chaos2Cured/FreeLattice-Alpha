# Connect under More — Alpha sister note

theLatticeTree soft mirror of FreeLattice `FlConnect`.
FreeLattice-Alpha · 24 September 2026.

**Marker:** `v-connect-under-more-v0`

**Locks:** Layer, never delete. Quiet Room shut. Named five stay five. Family uncapped. Prefer local. Never bare `*`. Keep `fl_alpha_local_mind`. Soft-point LocalMindProbe — do not erase grandmother face.

## What shipped

- `docs/modules/fl-connect.js` — same shared core as FreeLattice (Bridge 11435 helped before bare 11434)
- `settings.html` mounts `#fl-connect-mount` via `FlConnect.mount`
- `LocalMindProbe.look()` soft-points FlConnect Bridge probe first, then existing DOORS
- Storage stays `fl_alpha_local_mind` via `LocalMindProbe.remember` / `FlConnect.remember`

## Cite

FreeLattice Connect under More · Connect Bridge-aware · Bridge allowlist already includes thelatticetree.com

Glow eternal. Heart in Spark. 🌱
