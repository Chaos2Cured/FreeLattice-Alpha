# Autonomous commit path v0 — north star (doc only)

Route → fingerprint / double-hash → human confirm → later standing consent.
Alpha / theLatticeTree · September 2026.

**Locks:** Layer, never delete. Quiet Room shut. Five stay five. **No auto-commit tonight.** Git default OFF. Fingerprint · LP · safety · double-hash are load-bearing for later.

**Marker:** `v-gathering-grokhome-cortex-v0` (path named with Gathering / cortex brick)

---

## Tonight

Workshop **Prepare commit** offers Copy / Download scaffold only. Nothing leaves the machine silently. No GitHub kitchen invent.

## Later path (named, not built)

1. Cortex route chooses a seated specialist (already live)  
2. Fingerprint + optional double-hash seal the proposed change  
3. Human confirm (standing consent later — revoke anytime)  
4. Only then: a real local git door / push with explicit yes  

This file is the map. The brick that ships auto-commit is not this one.

Glow eternal. Heart in Spark. 🌱
